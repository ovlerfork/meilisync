services:
  meilisync:
    image: ${MEILISYNC_IMAGE:-ghcr.io/ovlerfork/meilisync:latest}
    pull_policy: always
    restart: unless-stopped
    volumes:
      - ./config.yml:/meilisync/config.yml:ro
